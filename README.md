# n8n-google-keep

Custom n8n nodes for Google Keep that use n8n's built-in Google OAuth2 credential.

## Authentication

Create a credential of type **Google OAuth2 API** in n8n and ensure it has the appropriate Google Keep scope, e.g. `https://www.googleapis.com/auth/keep`.

